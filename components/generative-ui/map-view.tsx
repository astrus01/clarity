"use client";

// Stub — fill in with Mapbox GL during hackathon
export function MapView({ props }: { props: any }) {
  return (
    <div className="h-64 rounded-lg border border-border bg-surface flex items-center justify-center text-foreground-muted text-sm">
      Map placeholder — Mapbox GL
    </div>
  );
}
