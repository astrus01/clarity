"use client";

// Stub — fill in with React Three Fiber during hackathon
export function Scene3D({ props, children }: { props: any; children?: React.ReactNode }) {
  return (
    <div className="h-80 rounded-lg border border-border bg-surface flex items-center justify-center text-foreground-muted text-sm">
      Scene3D placeholder — React Three Fiber
    </div>
  );
}
